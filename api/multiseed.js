function rng32(a){return()=>{let t=a+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
function run(seed,cycles,mode,fault){const r=rng32(seed);let q=Array.from({length:16},(_,i)=>({f:.997-r()*.001,n:.00025+r()*.0001,d:0})),events=0,min=1;for(let t=0;t<cycles;t++)q.forEach((x,i)=>{x.d+=(r()-.5)*.00012;x.n=Math.max(.0001,x.n+(r()-.5)*.00003);if(t===Math.floor(cycles*.25)&&i===seed%16)x.d+=fault;x.f=Math.max(.96,.9975-Math.abs(x.d)-x.n);if(mode==="qart"){let risk=Math.max(0,.995-x.f)*120+Math.max(0,Math.abs(x.d)-.001)*100;if(risk>.48){x.d*=.15;x.n=Math.max(.0002,x.n*.6);x.f=.9975-Math.abs(x.d)-x.n;events++}}min=Math.min(min,x.f)});return{f:q.reduce((a,x)=>a+x.f,0)/16,a:q.filter(x=>x.f>=.992).length/16,e:events,min}}
const mean=a=>a.reduce((x,y)=>x+y,0)/a.length;
export default function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"POST required"});let b=req.body||{},start=Math.max(1,Number(b.startSeed)||1000),n=Math.max(2,Math.min(100,Number(b.seeds)||25)),cycles=Math.max(20,Math.min(2000,Number(b.cycles)||600)),fault=Math.max(0,Math.min(.02,Number(b.fault)||.007)),rows=[];for(let i=0;i<n;i++){let seed=start+i,base=run(seed,cycles,"baseline",fault),qart=run(seed,cycles,"qart",fault);rows.push({seed,baseline:base,qart,delta:{f:qart.f-base.f,a:qart.a-base.a,min:qart.min-base.min}})}res.setHeader("Cache-Control","no-store");res.status(200).json({studyId:"QS-"+start+"-"+n+"-"+cycles,claimBoundary:"simulation",startSeed:start,seeds:n,cycles,fault,summary:{baselineMeanFidelity:mean(rows.map(x=>x.baseline.f)),qartMeanFidelity:mean(rows.map(x=>x.qart.f)),meanFidelityDelta:mean(rows.map(x=>x.delta.f)),meanMinFidelityDelta:mean(rows.map(x=>x.delta.min)),baselineAvailability:mean(rows.map(x=>x.baseline.a)),qartAvailability:mean(rows.map(x=>x.qart.a)),meanQartInterventions:mean(rows.map(x=>x.qart.e))},rows})}
